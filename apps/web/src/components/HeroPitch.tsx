import React from "react";
import TacticsPitch from "./TacticsPitch";

const HeroPitch: React.FC<{ active?: boolean }> = () => {
  return <TacticsPitch />;
};

export default HeroPitch;
export { TacticsPitch };

